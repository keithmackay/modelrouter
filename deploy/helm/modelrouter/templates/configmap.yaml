apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "modelrouter.fullname" . }}-config
  labels:
    {{- include "modelrouter.labels" . | nindent 4 }}
data:
  config.toml: |
{{ .Values.config | indent 4 }}
